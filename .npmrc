# Use flat (hoisted) node_modules layout so Stryker's child processes
# can resolve plugins like @stryker-mutator/vitest-runner.
# pnpm's default virtual store (.pnpm) isolates packages so strictly
# that Stryker workers cannot find peer plugins.
node-linker=hoisted
