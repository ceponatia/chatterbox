{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "lint:workspace",
      "type": "shell",
      "command": "pnpm -w exec eslint -f stylish -- apps/ packages/",
      "problemMatcher": ["$eslint-stylish"],
      "group": "build",
      "presentation": { "reveal": "always", "panel": "dedicated" }
    }
  ]
}
